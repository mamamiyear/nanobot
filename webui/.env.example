# Public mount path used when building and serving this WebUI instance.
VITE_BASE_PATH=/

# Gateway origin used by the Vite development proxy (do not include the base path).
NANOBOT_API_URL=http://127.0.0.1:8765
